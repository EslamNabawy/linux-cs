{
  "name": "linux101",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:data": "node scripts/build-data.mjs && node scripts/gen-lite.mjs && node scripts/build-content.mjs"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
